/* eslint-disable no-unused-vars */
import Choices from 'choices.js';

const choices = new Choices('.header-top__select', {
  searchEnabled: false,
  itemSelectText: '',
  allowHTML: false,
});

const choicesTwo = new Choices('.header-bottom__select', {
  searchEnabled: false,
  itemSelectText: '',
  allowHTML: false,
});
